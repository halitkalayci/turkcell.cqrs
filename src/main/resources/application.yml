spring:
  application:
    name: turkcellcqrs
  datasource:
    url: "jdbc:postgresql://localhost:5432/lms"
    username: postgres
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    showSql: true
    hibernate:
      ddlAuto: update
